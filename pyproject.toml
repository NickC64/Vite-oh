[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vite-oh"
version = "1.0.0"
description = "A serverless anonymous-veto Discord bot"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
  "fastapi==0.139.2",
  "google-cloud-firestore==2.28.0",
  "google-cloud-tasks==2.23.0",
  "httpx==0.28.1",
  "pydantic-settings==2.14.2",
  "pynacl==1.6.2",
  "uvicorn[standard]==0.51.0",
]

[dependency-groups]
dev = [
  "mypy==2.3.0",
  "pip-audit==2.10.1",
  "pytest==9.1.1",
  "pytest-asyncio==1.4.0",
  "pytest-cov==7.1.0",
  "respx==0.23.1",
  "ruff==0.15.22",
]

[tool.hatch.build.targets.wheel]
packages = ["viteoh"]

[tool.ruff]
line-length = 88
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC", "RUF"]

[tool.mypy]
python_version = "3.13"
strict = true
packages = ["viteoh"]
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = "google.protobuf.*"
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "--cov=viteoh --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
omit = [
  "viteoh/register_commands.py",
  "viteoh/repository.py",
]
